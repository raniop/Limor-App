# macOS
.DS_Store

# Xcode build / user state
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/

# Swift Package Manager
.swiftpm/
.build/
Packages/
Package.resolved

# Backup directories
*.template-backup/

# Claude Code local overrides
.claude/settings.local.json
# Spawned agent worktrees — embedded git repos, never commit these.
.claude/worktrees/

# Regenerated on every build by tools/inject-git-sha.rb.
# Committed once with a "dev" placeholder so a fresh clone compiles
# before the first build runs the script.
Limor Ai App/BuildInfo.swift
